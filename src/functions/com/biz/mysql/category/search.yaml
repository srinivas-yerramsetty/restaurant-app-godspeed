summary: Fetch many Category
tasks:
  - id: mysql_category_search
    fn: com.gs.datastore
    args:
      datasource: mysql
      data: <% inputs.body %>
      config:
        method: Category.findMany
